name: "Match All Logs"
type: any
index: logs-endpoint.events.process-*

alert:
  - debug

filter:
  - query:
      match_all: {}

alert_text: |
  Log matched (match_all).

realert:
  minutes: 0

priority: 1
