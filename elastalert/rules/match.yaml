name: "Match All Logs"
type: any
index: logs-endpoint.events.process-*

alert:
  - debug

filter:
  - query:
      match_all: {}

alert_text: |
  Log matched (match_all).

realert:
  minutes: 0

max_hits: 10  # ⚠️ Số lượng log tối đa được xử lý để alert
priority: 1
